

	//IMPORT DATA SNIPPET
	
				// for (i in allSkulls){
				// 	currSkull = allSkulls[i];
				
				// 	currSkull.save(function(err){
				// 		if(err){
				// 			console.error('error on save');
				// 		} else {
				// 			console.log('skull candle count added to db');
				// 		}
				// 	});
				// };