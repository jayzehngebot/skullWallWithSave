	
				// for (i in allSkulls){
				// 	//console.log(allSkulls[i].slug);
				// 	if (allSkulls[i].slug === undefined) {
				// 		allSkulls[i].slug = (allSkulls[i].name.toLowerCase().replace(/[^\w ]+/g,'').replace(/ +/g,'_'));
				// 		console.log('slug : ' + allSkulls[i].slug);
				// 	} else if (allSkulls[i].slug == ''){
				// 		allSkulls[i].slug = ('noname'+i);
				// 	} else {
				// 		console.log('slug : ' + allSkulls[i].slug);
				// 	}
				// };

				// for (i in allSkulls){
				// 	currSkull = allSkulls[i];
				// 	currSkull.save(function(err){
				// 		if(err){
				// 			console.error('error on save');
				// 		} else {
				// 			console.log('skull slug nonameNumber added to db');
				// 		}
				// 	});
				// };